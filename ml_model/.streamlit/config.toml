# Streamlit config to allow embedding the app in an iframe from another origin (Vite dev server)
[server]
headless = true
port = 8501
enableCORS = false
enableXsrfProtection = false

[browser]
serverAddress = "localhost"
