Small notes for `forecast` app

- Endpoint: `/api/weather/?lat=&lon=`
- Uses `requests` to fetch data from OpenWeather One Call API and returns a concise JSON payload.
- Keep code simple and readable; add logging/metrics as required by production needs.
